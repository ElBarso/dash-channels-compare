# Project Name: Interactive dashboard for sensors channels comparison
# Description: Create an interactive dashboard to compare the output of a four-channel sensor. 
# Separately plots each channel curves. Through a dedicated interactive plot, 
# enables direct comparison of tracks from different channels selecting them with a simple click.

# Core Dependencies
dash==2.13.0
numpy==1.22.3
plotly==5.13.0
pandas==1.4.2
scipy==1.8.0
